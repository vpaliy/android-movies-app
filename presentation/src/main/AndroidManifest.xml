<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.popularmovies.vpaliy.popularmoviesapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".App"
        android:theme="@style/PopMovies">

        <activity
            android:name=".ui.activity.SplashActivity"
            android:theme="@style/SplashTheme"
            android:noHistory="true"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.MediaActivity"
            android:theme="@style/PopMovies"/>

        <activity
            android:name=".ui.activity.MoreMediaActivity"
            android:theme="@style/PopMovies.MoreMedia"/>

        <activity android:name=".ui.activity.DetailsActivity"
                  android:theme="@style/PopMovies.Details"/>

        <activity
            android:name=".ui.activity.SettingsActivity"
            android:theme="@style/PopMovies"
            android:parentActivityName=".ui.activity.MediaActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.MediaActivity"/>
        </activity>

        <provider
            android:exported="false"
            android:authorities="com.popularmovies.vpaliy"
            android:name="com.popularmovies.vpaliy.data.source.local.MovieProvider"/>
    </application>
</manifest>